# Super Slippery Jelly Fish (SSJF)

A fun simple PEAK mod to make the jellyfish more slippery.

## Project setup

- Install the .NET SDK (8.x+ recommended).
- Copy the required PEAK/BepInEx DLLs into `../lib/` (see `../lib/README.md` for the list).
- Run `dotnet build`.
